import React, { useEffect } from 'react'
import { WatermarkProps } from '.';
type Props = {}

type WaterProps = Omit<WatermarkProps, 'children' | 'className' | 'style'>
export default function useWatermark({}: Props) {

  const {}  = props;

   const generatorWater = () => { 

    }
   useEffect(()=>{
generatorWater()
   },[options])
const getCanvasData = (second) => { third }
return {
generatorWater
}
}